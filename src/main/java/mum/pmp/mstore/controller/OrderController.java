package mum.pmp.mstore.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;



@Controller
public class OrderController {
	
	/*
	 * @Autowired OrderService orderService;
	 * 
	 * @GetMapping(value="/order") public String categoriesList(Model model) {
	 * model.addAttribute("ordersList", orderService.getOrders());
	 * return"order/ordersList";
	 * 
	 * }
	 * 
	 * 
	 * 
	 * 
	 * @PostMapping("/addOrder") public String addOrder(Model model, ShoppingCart
	 * shoppingCart) {
	 * 
	 * model.addAttribute("order", orderService.createOrder(shoppingCart));
	 * 
	 * return "user/userEmail"; }
	 * 
	 * 
	 * 
	 * 
	 * @GetMapping(value={"/ordersEdit","/ordersEdit/{id}"}) public String
	 * editOrder(Model model, @PathVariable(required = false, name = "id") Long
	 * orderNumber) { if (null != orderNumber) { model.addAttribute("orders",
	 * orderService.getOrder(orderNumber)); } else { model.addAttribute("orders",
	 * new Order()); } return "order/ordersEdit"; }
	 * 
	 * 
	 * 
	 * 
	 * @PostMapping("/customer/{orderNumber}/{email}") public String
	 * addCustomer( @PathVariable Long orderNumber, @PathVariable String email) {
	 * 
	 * orderService.addCustomer(orderNumber, email);
	 * 
	 * return "order/ addressShipping"; }
	 * 
	 * 
	 * @PostMapping(value="/addressEdit/{orderNumber}") public String
	 * addBillingAddress(Model model, @PathVariable Long orderNumber, Address
	 * address){
	 * 
	 * orderService.addBillingAddress(orderNumber, address); return
	 * "order/addressBilling"; }
	 * 
	 * 
	 * 
	 * @PostMapping(value="/addressEdit/{orderNumber}") public String
	 * addShippingAddress(Model model, @PathVariable Long orderNumber, Address
	 * address){
	 * 
	 * orderService.addShippingAddress(orderNumber, address); return
	 * "order/ordersList"; }
	 */
	 
	
	
	
	
	
	/*
	 * 
	 * @PostMapping("/")
	 * 
	 * @PostMapping(value="/categoriesEdit") public String addCategory(Model model,
	 * Category category) { categoryService.addCategory(category);
	 * model.addAttribute("categoriesList", categoryService.getCategories()); return
	 * "category/categoriesList"; }
	 * 
	 * 
	 * 
	 * @Autowired CategoryService categoryService;
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 * @GetMapping(value="/category") public String categoriesList(Model model) {
	 * model.addAttribute("categoriesList", categoryService.getCategories()); return
	 * "category/categoriesList"; }
	 * 
	 * 
	 * @GetMapping(value={"/categoriesEdit","/categoriesEdit/{id}"}) public String
	 * editCategory(Model model, @PathVariable(required = false, name = "id")
	 * Integer id) { if (null != id) { model.addAttribute("categories",
	 * categoryService.getCategory(id)); } else { model.addAttribute("categories",
	 * new Category()); } return "category/categoriesEdit"; }
	 * 
	 * 
	 * @PostMapping(value="/categoriesEdit") public String addCategory(Model model,
	 * Category category) { categoryService.addCategory(category);
	 * model.addAttribute("categoriesList", categoryService.getCategories()); return
	 * "category/categoriesList"; }
	 * 
	 * 
	 * @RequestMapping(value="/categoriesDelete/{id}", method=RequestMethod.GET)
	 * public String deleteCategory(Model model, @PathVariable(required = true, name
	 * = "id") Integer id) { categoryService.deleteCategory(id);
	 * model.addAttribute("categoriesList", categoryService.getCategories()); return
	 * "category/categoriesList"; }
	 */
	
	
	
	
	

}
